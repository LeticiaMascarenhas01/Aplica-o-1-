import { title } from "process"

const CreatGoalRequeste {
    desireWeekFrequency: number
} 

export async function CreatGoalR({title, desireWeekFrequency}: CreatGoalRequeste)}
await db.insert(goals).value({
    title,
    desireWeekFrequency,
  )}
  .returne()

  const goal = result[0]

  return {
    goal.
     
  }
}